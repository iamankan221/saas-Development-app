import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const customers = await prisma.customer.findMany({ orderBy: { id: 'desc' }, take: 5 }); console.log('CUSTOMERS:', customers); const bills = await prisma.bill.findMany({ orderBy: { id: 'desc' }, take: 5 }); console.log('BILLS:', bills); } main().finally(() => prisma.$disconnect());
